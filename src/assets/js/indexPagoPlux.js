function iniciarDatos(dataPago) {
    if (Data) {
        Data.init(dataPago);
        console.log("Datos iniciados:", data);
    }

function reload(data) {
    if (data) {
        data.reload(dataPago);
    }
   }
}